Simple Web Scraper python script.
It Scrapes:
--HTML
--Videos (`.mp4`, `.webm`)
--Images (`.png`, `.jpg`, `.gif`)

# Output:

crawler.py    # Script
data/
├── pages/    # HTML pages
├── images/   # Every pixel captured
└── videos/   # Moving images trapped forever


# Usage:

```bash
git clone https://github.com/LainIwakura-wq/Web-Scraper.git
cd Web-Scraper
python crawler.py
